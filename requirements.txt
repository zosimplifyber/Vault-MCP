# Vault MCP Server dependencies
# (Test-only deps live in requirements-dev.txt — pytest + pytest-asyncio.)
mcp[cli]>=1.0.0          # Model Context Protocol SDK (includes FastMCP + SSE transport)
httpx>=0.27.0            # Async HTTP client for Vault REST API calls
uvicorn[standard]>=0.30.0  # ASGI web server
pypdf>=4.0.0             # PDF read/write/merge for the watermark tool
reportlab>=4.0.0         # Generates the watermark overlay layer
pandas>=2.0.0            # BOM tabular processing for the purchasing-sheet tools
openpyxl>=3.1.0          # Writes the formatted .xlsx purchasing sheets
xlrd>=2.0.0              # Lets pandas read legacy .xls Vault BOM exports
msal>=1.30.0            # Microsoft auth (device-code) for the purchasing Microsoft-List reference
requests-pkcs12>=1.24   # McMaster OFFICIAL API — attaches the .pfx client cert (recommended path)
playwright>=1.40.0      # McMaster browser fallback ONLY (opt-in via --allow-scrape; run `playwright install chromium`)
