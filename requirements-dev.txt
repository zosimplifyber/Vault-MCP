# Dev/test-only dependencies (not needed to run the servers).
# Install with:  pip install -r requirements-dev.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
reportlab>=4.0.0         # Builds PDFs in-memory for the OpenDataLoader router tests
